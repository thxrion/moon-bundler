__MODULES = {}

local __require = require
function require(path)
    local module = __MODULES[path]
    if module then
        return module()
    end

    return __require(path) 
end

__MODULES["path/to/file"] = function()
   print("module loaded...")
end

require("path/to/file")